# Legacy Server Files

This folder contains archived server-side files that are no longer used in the active dashboard runtime.

The dashboard currently uses:

- `server/index.ts`
- `server/db.ts`
- `server/routers/`
- `server/websocket.ts`

Archived file:

- `storage.ts` (older in-memory storage implementation)
